# Follow-up regression matrix

**Status:** The offline suite ran (92 passed). EVM tests below were written or updated but not executed in this environment. “Before/after” describes expected behavior to establish with real RED/GREEN runs, not an executed exploit witness. TypeScript transpilation is syntax-only.

| Path                             | Before / defect to reproduce                                               | Required new behavior                                                                                                                    | Test location                                                                             |
| -------------------------------- | -------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| Permissionless keeper auction    | Previous audit incorrectly treated open execution as unauthorized          | Arbitrary funded payer succeeds with fee and no outer-share mint; config remains privileged                                              | `test/followup-2026-09-05/RewardSettlement.test.ts` and existing MetaMorpho tests         |
| Wrapper holder rewards           | Aggregate controller claim skips shareholder accounting                    | Collateral holder receives exactly its entitlement; independent holder remains fully payable; precollected/stopped-index rewards recover | `RewardSettlement.test.ts` (real repository wrapper, deterministic controller/pool mocks) |
| Reward risk boundary             | Legacy manager deposits directly despite current router control changes    | Pause, current router, Active strategy, default, cap and shared backing checks enforced atomically                                       | `RewardSettlement.test.ts`                                                                |
| Honest inner conversion rounding | One-base-unit policy rejects the demonstrated two-unit whole-position loss | Explicit reviewed two-unit policy admits the exact honest case; default one still rejects                                                | `RoundingAndLifecycle.test.ts`                                                            |
| Repeated legs / fake backing     | Per-leg tolerance could hide multiplied loss or zero added backing         | Aggregate budget rejects three-unit total loss under budget two; zero gain still rejects at allowance 16                                 | `RoundingAndLifecycle.test.ts` and original backing regression suite                      |
| Funded strategy lifecycle        | Delisting excludes shares from NAV and permits redistribution on relisting | Retained configurations preserve NAV; funded removal reverts; explicit bounded dust disposal only                                        | `RoundingAndLifecycle.test.ts`, `RouterGovernanceFlows.test.ts`, Foundry handler          |
| Curve supplied user              | Outsider can spend a victim's adapter approval                             | Reject before any pool/permit/transfer/flash action; self user reaches protocol                                                          | `PeripheryAndOracle.test.ts`; positive full-route fork tests still needed                 |
| AMO repayment                    | Protected permit entry re-enters another protected method                  | Real permit and existing allowance scenarios use one guard; all auth retained                                                            | Existing `test/dstable/AmoManagerV2.spec.ts` with corrected `borrowTo` setup              |
| Composite freshness              | Newer component timestamp conceals older dependency                        | Min timestamp in both feed orders; zero/future/stale values reject                                                                       | `PeripheryAndOracle.test.ts`                                                              |
| Capability retirement            | Old manager adapter/admin/custody/claimer persists after router switch     | Atomic migration fails until every listed capability is retired; separate-owner revocation is a verified precondition                    | Three new cases in `RouterIncidentMigration.test.ts`                                      |
| Deployment safety                | Historical deployment names and incomplete retirement allow silent reuse   | Versioned identity, paused deployment, immutable guard inventory, reviewed policy, no unknown claimer overwrite or automatic reopening   | Offline policy tests; actual artifact/fork validation still needed                        |

New Hardhat file cases: 11 reward settlement, 9 rounding/lifecycle, 11 periphery/oracle = **31** parameter-expanded cases. Three additional migration cases = **34 new cases**. Existing case corrections and Foundry handler updates are not counted as new passing tests.

The default validation script runs all existing Hardhat tests and dSTAKE Foundry tests after compilation. Do not replace them with the small new suite, change legitimate economic expectations simply to make them pass, disable size limits or skip failed fork integrations without explicitly retaining a release blocker.
